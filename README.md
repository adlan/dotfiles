# dotfiles

## Requirements

1. GNU Stow

## Install

Clone this repo.

```shell
git clone git@github.com:adlan/dotfiles.git ~/dotfiles
```

Run stow inside the cloned repository.

```shell
cd ~/dotfiles
stow .
```
